[build]
  publish = "."

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[functions."ask-npc"]
  node_bundler = "esbuild"

[functions."score-background"]
  node_bundler = "esbuild"

[functions."check-status"]
  node_bundler = "esbuild"
